# Project conventions

## Language

All code, comments, UI copy, commit messages, and documentation must be written in **English**, unless the user explicitly asks for another language in that specific request.

Exception: content that is inherently bilingual/localized by design (e.g. the Ukrainian quiz questions/answers/jokes in `src/quizzes.js`) stays as requested — this rule targets code/UI text, not intentionally localized data.
